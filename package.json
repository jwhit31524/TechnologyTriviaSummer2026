{
  "name": "vscode-trivia-game",
  "version": "1.0.0",
  "description": "Technology Trivia Game — MVC Architecture",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently -n server,client -c cyan,magenta \"cd server && node --env-file=.env server.js\" \"cd client && npm run dev\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "concurrently": "^8.2.2"
  }
}
